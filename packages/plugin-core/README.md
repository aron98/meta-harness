# @meta-harness/plugin-core

Bootstrap package for the host-neutral adapter contract boundary. It is intentionally minimal in this slice and exists to give later adapter work a stable workspace package to build on.
